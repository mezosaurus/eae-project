using UnityEngine;
using System.Collections;

public class EyeFlameScript : MonoBehaviour {

	// Use this for initialization
	void Start ()
    {
        particleSystem.renderer.sortingLayerName = "Default";
	}
	
	// Update is called once per frame
	void Update ()
    {
	    //if(particleSystem.isPlaying)
	}
}
